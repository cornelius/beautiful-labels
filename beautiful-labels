#!/usr/bin/env python3

import sys

import scanner

if len(sys.argv) != 3:
    print("Usage: beautiful-labels scan <org> <repo>", file=sys.stderr)

org = sys.argv[2]
repo = sys.argv[3]

labels = scanner.scan_labels(org, repo)
scanner.write_yaml(labels, org + '-' + repo + '-labels.yaml')
